import React from 'react';
import Button from '@material-ui/core/Button';

function Login() {
    return (
        <div>Hello world Login
            <Button variant="contained" color="primary" >
        Primary
      </Button>
        </div>
    );
}

export default Login;
